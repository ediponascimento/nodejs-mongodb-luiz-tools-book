<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="stylesheets/style.css">
  <title>Crud Clientes | Cadastrar</title>
</head>

<body>
  <h1><%= title %></h1>
  <p>Preenhcer os dados abaixo para salvar o cliente</p>
  <form action="<%= action%>" method="POST">
    <p>
      <label for="nome">Nome:</label>
      <input type="text" name="nome" value="<%=doc.nome%>">
    </p>
    <p>
      <label for="idade">Idade:</label>
      <input type="number" name="idade" value="<%=doc.idade%>">
    </p>
    <p>
      <label for="uf">UF<select name="uf">
          <% const s = "selected"%>
          <option <%if(doc.uf === "RS"){%><%= s %><% }%>>RS</option>
          <option <%if(doc.uf === "SC"){%><%= s %><% }%>>SC</option>
          <option <%if(doc.uf === "PR"){%><%= s %><% }%>>PR</option>
        </select>
      </label>
    </p>
    <a href="/">Cancelar</a> | <input type="submit" value="Salvar">
  </form>
</body>

</html>